syntax = "proto3";

package record.pb;

import "crypto/pb/crypto.proto";

// TODO(yusef): doc comments before merge
message Envelope {
    crypto.pb.PublicKey publicKey = 1;
    bytes payloadType = 2;
    bytes payload = 3;
    bytes signature = 5;
}
